#
# Configuration file for use in subsequent curl command files.
#
# Edit this file to configure your environment.
# APIs will read this in and be directed to the url you enter below, using Basic Authentication as per the pwd.
# 
# pwd is Basic Authentication - to generate the string use the command below:-
#
# echo -n 'username:password' | base64 
# 
URL="https://<IP>>:<END_USER_HTTP_PORT>>/api/v2.0"
PWD="..."
